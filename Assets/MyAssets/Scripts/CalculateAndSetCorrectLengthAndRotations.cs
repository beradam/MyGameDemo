using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CalculateAndSetCorrectLengthAndRotations : MonoBehaviour
{

    //Vector3 v3A = GetStartPos();
    //Vector3 v3B = GetEndPos();

    //// Start is called before the first frame update
    //void Start()
    //{
    //    // Position the gameobject properly
    //    PositionSize(v3A, v3B);
    //}

    //void PositionSize(Vector3 v3Start, Vector3 v3End)
    //{

    //    var v3Pos = (v3End - v3Start) / 2.0f + v3Start;  // Position
    //    transform.position = v3Pos;

    //    var v3 = v3End - v3Start;    // Rotation
    //    transform.rotation = Quaternion.FromToRotation(Vector3.right, v3);

    //    transform.localScale = new Vector3(v3.magnitude, 0.1f, v3.magnitude);  // Scale 
    //}
}
